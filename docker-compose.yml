services:
  django:
    build: ./django
    ports:
      - "8000:8000"
    volumes:
      - ./django/project:/app
    command: python manage.py runserver 0.0.0.0:8000

  n8n:
    image: n8nio/n8n
    ports:
      - "5678:5678"
    volumes:
      - n8n_data:/home/node/.n8n

volumes:
  n8n_data: